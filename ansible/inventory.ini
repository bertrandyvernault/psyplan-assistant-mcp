# Inventory file not used in CI/CD.
# The host is passed directly via -i "host," in the GitHub Actions workflow
# to avoid Jinja2 template parsing issues in .ini format.
#
# For local manual runs:
# ansible-playbook ansible/deploy.yml -i "YOUR_VPS_IP," --user YOUR_USER --private-key ~/.ssh/deploy_key
